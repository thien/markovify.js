class Chain {
    // data is array of strings
    var corpus = {};
    constuctor(data) {
        for (var i = 0; i < data.length; i++) {
            if (typeof corpus[data[i]]) {
            }
        }
    }
}